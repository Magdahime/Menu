#ifndef MAIN_HPP
#define MAIN_HPP
#include <iostream>
#include <stdbool.h>
#include <vector>
#include <array>
constexpr int COMMANDS={16};
constexpr int MAXLINE={80};
constexpr int WIDTH={5};
constexpr int ERROR={-1};
const std::string stars(MAXLINE, '*');
#endif
